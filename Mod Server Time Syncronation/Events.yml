# Syncs the world time to the servers real time.
Auto Sync:
    debug: false
    type: world
    delay: d@1m
    
    events:
        on server start:
        - while true {
            - run "Scripted Time Changer"
            - wait <script.yaml_keys[delay]>
            }
