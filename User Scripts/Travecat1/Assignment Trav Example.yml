Travs Npc Walk Close:
    type: assignment
    list:
    -
    -
    -
    actions:
        on assignment:
        - lookclose <npc>           state:true
        - trigger name:chat         state:true
        - trigger name:proximity    state:true
        - chat "You might want more triggers (like click triggers too)"
        
        
        # dont follow this code as im talking about what I will be doing here
        - if <player.name> == Rag1ngW1f3 chat "This is 1 argument for the chat command"
        - if <player.name> == Chemical_Datas {
            - chat "This is a multi line argument"
            - chat Notice_how_its_indented_and_Braced.....And_how_this_is_still_1_argument
            - execute as_op "announce .<&bs>kick <player.name>"
            }
        
        on chat:
        -
        -
        -


        on spawn:
        -
        -
        -
        
        on enter proximity:
        - chat "HI There!"
        -
        -