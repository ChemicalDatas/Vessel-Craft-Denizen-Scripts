CUI Display Event:
    type: world
    events:
        on player clicks block with wood_axe:
        - if <player.selected_region||no_region> == no_region queue clear
        - showfake m@glowstone <player.selected_region.outline> to:<player>
        #- showfake m@glass <player.selected_region.get_blocks[m@air]>

