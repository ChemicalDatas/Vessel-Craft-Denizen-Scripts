Bungee Announce:
    type: world
    events:
        # Events
        # On server Join for new announcements
        # Random announcements
        on server start:
        #on announce command:
        #- determine passively fulfilled
        #- bungee all {
        #    - announce "<parse:<proc[iconize].context[<context.args.space_separated>]>>"
        #    }