# A set of procedures to aid in script creation

  
"Remaining Iterator":
    type: procedure
    definitions: current|max
    script:
    - if <def[current]> < <def[max]> {
        - determine true
        - else determine false
        }

# The iterator name needs to be held outside of this object
"Yield Iterator Incrementation":
    type: procedure
    definitions: list|unique_iterator_name
    script:
    - define index <global.flag[<def[unique_iterator_name]>]>
    - define yield <<def[list]>.get[<def[index]>]>
    - flag global <def[unique_iterator_name]>:++
    - determine <def[yield]>